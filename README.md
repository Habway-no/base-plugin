# Base Plugin for Arcturus Morningstar 3.5.1

#### Requires:
* Java
* Maven

#### How to setup:
* Change all `no.package.name` references in the project to your package name
* Start Coding in the `Main.class` file.
* When done coding, run `mvn package` in your terminal.